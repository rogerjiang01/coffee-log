# 手沖咖啡紀錄平台

## 規格文件

開始任何工作前，先讀 `docs/` 底下三份文件：

- `docs/01-資料庫規格.md` — 資料結構，權限最高
- `docs/02-功能規格.md` — 頁面、流程、互動行為
- `docs/03-介面規範.md` — 視覺方向與元件規範
- `docs/04-詞彙表.md` — 介面與規格用詞，含刻意不用的同義詞

三者衝突時，以 01 為準。

## 技術棧

Nuxt 3 + Vue 3 + TypeScript + Tailwind CSS
Supabase（Auth / Postgres / Storage）
部署於 Vercel

## 硬性規則

1. 不得新增規格未定義的資料表、欄位、頁面、功能
2. 不得實作《02-功能規格》第 11 節列出的任何 V2 項目，即使實作成本很低
3. 不得將衍生值存成資料庫欄位（清單見《01-資料庫規格》第 8 節）
4. 所有使用者資料表必須有 RLS policy，沒有 policy 的表視為未完成
5. `brew_steps` 必須是獨立資料表，不得改用 JSONB 或文字欄位
6. 分段注水的時間是「停水時間」（秒；注完到下一注之間，不含注水動作）：介面填什麼就存什麼，不得再加任何換算層；最後一段不存（NULL）
7. 必填欄位只有三個：`beans.name`、`brews.bean_id`、`brews.dose`，不得對其他欄位加必填驗證
8. 不得使用 UI 框架的預設外觀直接交付
9. 不得使用瀏覽器原生的 `alert`、`confirm`、`prompt`
10. 遇到規格沒寫的情況，**停下來詢問，不要自行決定**

## 工作方式

- 一次只做一個階段，做完停下來讓我驗收，不要自動接續下一階段
- 不要主動重構其他階段已完成的程式碼
- 每個階段結束前，明確列出：做了什麼、跳過了什麼、有哪些地方是你自行判斷的
- Supabase schema 一律透過 `supabase/migrations/` 的檔案管理，不得假設我會在 dashboard 手動設定
- **含不可逆操作的 migration，在要我跑 `supabase db push` 之前必須先報告。** 不可逆指
  `drop column`、`drop table`、以及任何覆寫既有使用者資料的 `update`／`delete`。
  報告要有三項，寫在請我執行的那則訊息裡，不要只寫在 migration 註解：
  1. **哪幾行是不可逆的**，逐條列出（例如「第 62 行 `alter table brew_steps drop column time_offset`」）
  2. **影響範圍**：動到哪張表、哪些列、資料會怎麼變，以及搬移後對不上時 migration 會不會自己中止
  3. **備份指令**，可以直接複製貼上執行。**這台機器沒有 Docker，`supabase db dump` 跑不起來**
     （它靠 Docker 裡的 pg_dump），所以預設給一段在 Supabase SQL editor 執行的 `select`，
     讓它直接產生還原用的 `update`／`insert` 語句，存成檔案。只列會被動到的列，比整份 dump 好還原。
     Supabase CLI 是專案的 devDependency，沒有全域安裝：指令一律寫成 `pnpm exec supabase …`
  推上去之後才發現搬錯，能救回來的只有備份——遠端沒有復原點，
  而這個專案的資料是一筆一筆手動記的，重建不回來。
- **新增或修改任何面向使用者的文案前，先查 `docs/04-詞彙表.md`。** 表裡沒有的詞先問，
  不要自己挑一個同義詞；要換用詞，先改詞彙表再改介面。第三欄「刻意不用的同義詞」
  每一個都「也通」，所以最容易被順手改回去（`tests/unit/glossary.test.mjs` 會擋）
- 提交訊息使用正體中文

## 測試

```bash
pnpm test
```

測試放在 `tests/`，用 Node 內建的型別剝除直接跑，不引入測試框架——
這些測試的價值在於守住幾條不可回歸的規則，不需要 watch mode、快照或 mock。

| 位置 | 內容 |
|---|---|
| `tests/unit/` | 純函式：單位轉換、差異計算、模糊比對、刻度驗證、浮層定位、錯誤訊息對應 |
| `tests/db/` | 在 WASM 版 Postgres 上套用全部 migration，驗 schema、RLS、約束與 seed |
| `tests/helpers/pg.mjs` | 建立測試資料庫。stub 模擬 Supabase 平台既有的 auth／storage 與三個角色 |
| `tests/run.mjs` | 執行器 |

`tests/helpers/pg.mjs` 的 stub **刻意不預先授權 public schema**，
授權必須由專案自己的 grants migration 提供——這樣那支 migration 才真的被驗到。

### 不可回歸的關鍵案例

依重要性排序。改動這些行為時，測試必須跟著改而不是刪掉。

1. **分段注水的資料模型**（`tests/unit/brew-steps.test.ts`、`tests/db/brews.test.mjs`）
   「停留」欄位存的是**停水時間**（注完到下一注之間，不含注水動作），
   與累積水量一樣**原樣存、原樣讀，沒有換算層**。
   最後一段不存停留（沒有下一注），一律 NULL；沒記錄也是 NULL。
   關鍵斷言：`[45,30,30,40]` 存進去是 `hold_seconds [45,30,30,null]`，讀回來一致。

   **不要把換算層加回來。** 舊版存累積時間點（`time_offset`），停留被定義成
   「這段開始注水到下一段開始注水」的全部時間——那要求使用者提供他不知道的
   數字（注水花了幾秒），兩位實機測試者都填不出來。那層換算連同它的測試
   已經一起移除，它從頭到尾都在服務一個錯誤的欄位定義。
2. **資料隔離**（`tests/db/schema.test.mjs`）
   兩個帳號互相讀不到、改不動、刪不掉；混合表擋得住 `user_id = NULL` 的偽造。
3. **必填欄位只有三個**：`beans.name`、`brews.bean_id`、`brews.dose`。
   其餘欄位不得有 NOT NULL。
4. **衍生值不存資料庫**：`brews` 不得出現總水量、粉水比、養豆天數這類欄位。
5. **刻度驗證只是提示**：`increment` 為 null 不驗倍數、`max` 為 null 不驗上限、
   三者皆 null 完全自由。EK43 的 0.1 有浮點陷阱。
6. **浮層定位不得超出視窗**：超出的部分因為 `position: fixed` 捲不到，
   底部的動作會永遠點不到且不報錯。

---

## 溝通

- 使用正體中文
- 直接說明技術取捨，不需要鋪陳
- 不確定時直接問，不要猜測後繼續做

---

## 開發階段

**目前進行到：階段 10（視覺與文案收尾）**

完成一個階段就把狀態改為「完成」，並更新上面這一行。

| 階段 | 內容 | 狀態 | 驗收方式 |
|---|---|---|---|
| 0 | Nuxt + Supabase + Tailwind 初始化，建立設計 token | 完成 | 本機能跑起來，token 檔案存在 |
| 1 | 全部 migration ＋ RLS policy ＋ 基礎 seed | 完成 | 兩個測試帳號互相讀不到對方資料 |
| 2 | 註冊、登入、登出 | 完成 | 能完整走一遍 |
| 3 | 豆子 CRUD，含豆袋照片上傳 | 完成 | 只填豆名能存檔 |
| 4 | 器材 CRUD，含型錄選擇與預設值 | 完成 | 型錄與自建兩條路徑都能建立，切換預設不報錯 |
| 5 | 沖煮紀錄新增與編輯，含分段注水 | 完成 | 停水時間原樣存進 `hold_seconds`，最後一段留空；EK43（無段）能正常填刻度 |
| 6 | 複製流程 ＋ 差異計算與顯示 | 完成 | 複製改一個數字，詳情頁顯示差異 |
| 7 | 首頁（豆子卡片區 ＋ 時間軸） | 完成 | 375px 下滑順 |
| 8 | 豆子詳情頁與比較表 | 完成 | **375px 下實測橫向捲動與可讀性** |
| 9 | 表單自動暫存 | 完成 | 填一半重整，內容還在 |
| 10 | 視覺調整與收尾 | 進行中 | 對照《03-介面規範》第 1 節逐條檢查 |

### 待辦（記下來，之後處理）

- **階段 10**：重新評估攪拌標記是否重要到需要獨立圖示，或可併入備註。
  （已提出判斷依據，等你決定）決定時一併處理：它目前只有圖示，不在《03》§3 的
  圖示封閉清單裡（分段的 × 與器材選擇器的 ＋ 已列入清單）。

### V1 視覺待處理（凍結）

**凍結的理由**：進入 V2 後，不影響功能實作的視覺項目一律等功能定案後一次處理。
每輪各修一項的話，收斂沒有終點——V2 的導覽重整（紀錄獨立列表頁、首頁改儀表板）
會重畫一批畫面，現在修的東西有一部分會被那次改動推翻。

**凍結期間可以新增項目，但不主動修改；項目處理完成後才從清單移除。**
之後發現的同類項目記到這裡，不要順手修掉。

| # | 項目 | 不改會不會影響後續功能實作 |
|---|---|---|
| 1 | **攪拌標記是否需要獨立圖示**（見上面的待辦）。目前只有圖示、不在《03》§3 的封閉清單裡 | 維持圖示不影響。若決定改成併入備註，那不只是視覺項目——會動到 `brew_steps.stirred` 的寫入與顯示，要當功能改動排 |
| 2 | **強度與星等的 radiogroup 語意與鍵盤操作不一致**。`role="radiogroup"` 但五顆點是獨立按鈕：Tab 逐顆經過，方向鍵沒反應 | 不影響。改成方向鍵導覽是這兩個元件內部的事 |
| 3 | **分段刪除後焦點的去向沒驗過**。刪掉一段時那一列消失，焦點可能掉到 `<body>` | 不影響 |
| 4 | **`text-xs` 還有 40 處沒改成 `text-sm`**（20 個檔案）。`--text-xs` 已等於 `--text-sm`，畫面上沒有差別，純粹是名字還沒收乾淨（《03》§2.4） | 不影響。但 `--text-xs` 這個別名不能刪，刪了 `text-xs` 會靜默退回 Tailwind 內建的 12px |
| 5 | **《03》§1 逐條檢查裡需要人眼的兩項**：卡片邊界是否對應真實分組語意、配色有沒有滑向預設的咖啡美學。機械項目（全大寫英文、進場動畫、中間點 meta、裝飾性箭頭）已掃過，沒有違規 | 不影響。語意層 token 改值的成本與時間點無關 |
| 6 | **全站鍵盤走查沒做過**。《03》§7 的觸控目標與對比算過，焦點順序與可見性沒有系統性走過 | 不影響 |
| 7 | **器材型錄格式在差異區塊與比較表的長度沒在實機看過**（`磨豆機 Hario · V60 02 → Kalita · Wave 155`） | 不影響 |
| 8 | **沖煮表單的「內建沖煮手法為參考框架，實際水量請依自己的器材與豆子調整」**。當初決定保留到分段模板數值實機核實為止；模板數值至今仍是依網路資料的估算（見上面的階段備註） | 不影響。但它綁的是 seed 核實，不是視覺收尾——核實完才輪得到這句的去留 |
| 9 | **`IntensityPicker.vue` 的程式風格與其他元件不同**（四空格、雙引號、分號、長行）。來自 `d03e464`，該次唯一的實質改動是「弱／強」那一列 `mt-3` → `mt-5`。`DraftOverlay.vue` 同類，已決定不還原 | 不影響 |

### V2 待辦（階段 10 之後，不在本輪範圍）

- **沖煮紀錄的獨立列表頁，與首頁改成儀表板** — 目前沖煮紀錄沒有自己的列表頁，
  只存在於首頁時間軸與豆子詳情的比較表裡，所以它在導覽上沒有父層：返回的目標
  只能靠猜，這也是紀錄詳情的 ‹ 改成歷史式的原因（《03》§3）。
  規劃方向：首頁改成沖煮儀表板（統計與趨勢），紀錄獨立成一個分頁，階層變成
  「紀錄分頁 → 紀錄詳情」與「豆子分頁 → 豆子詳情 → 比較表」。那時 ‹ 可以改回
  階層式，而從豆子比較表進去的路徑交給瀏覽器返回鍵處理——那才是歷史式的情境。
  這個改動會一併解決導覽的根本問題，而不是繼續打補丁。
- **沖煮法管理頁面** — 使用者可自行新增沖煮手法（含分段模板），在沖煮表單中
  帶入。`brew_methods` 已是混合表（系統內建 ＋ 使用者自建），schema 不需要改，
  只缺管理介面。
- **欄位說明的教學層** — 以可點擊的小圖示提供領域知識（例如養豆天數為何重要、
  悶蒸水量該是粉重幾倍），而非操作說明。操作說明應由介面本身表達清楚，
  不放進提示。
- **拍照取景框** — 用 `getUserMedia` 做自訂相機預覽，疊一個與列表縮圖相同比例
  的框、框外壓暗，讓使用者在按下快門前就知道會留下什麼。目前的問題是他要拍過
  一次、看到被裁切之後才知道要怎麼拍。**iOS Safari 的相機權限與全螢幕行為
  需要先驗證**，那是這條的主要風險。
- **自訂沖煮法的輸入方式** — 輸入用固定值（20g 時是這樣沖），儲存用比例
  （`basis` ＋ `factor`），跟分段注水的單位轉換同一個模式。使用者腦中記的是
  固定值，要他填比例是強迫心算；但只存固定值的話換粉重就失效，等同於複製功能。
  另需一個「悶蒸水量跟著粉重變」的開關，預設開啟。
- **自訂項目升格的半自動化** — 目前靠手動跑 `supabase/queries/自建項目盤點.sql`。
  未來做成後台介面，自動列出高頻自建項目供人工挑選與正規化。
  **不做全自動升格**：自建名稱不規範，自動升格會把髒資料變成所有人看得到的
  權威項目。
- **Logo 設計** — 需要的尺寸：favicon 32px、PWA icon 180px（含 maskable）、
  應用內 header、錯誤頁。換掉時 `public/manifest.webmanifest` 與 `nuxt.config`
  裡硬寫的 `theme_color` 色碼要一併確認——那兩處吃不到 CSS 變數。
- **意見回饋** — 現階段不做進 app，測試者是朋友，直接問更有效。最便宜的版本是
  設定頁放一個外部表單連結。做進 app（帶上頁面路徑與錯誤代碼）等有真實流量之後。

### 階段備註

**階段 1 是最關鍵的一步。** 做錯後面全部要重來。完成後我會自己開兩個帳號實測資料隔離，不要只回報「已完成」。

seed 資料中，**磨豆機的刻度規格數值已核實並寫入**（《01》§10.1 的表格就是核實後的內容，`20260831161100_seed.sql` 依它建立，含楊家五台）。這一行原本寫「與分段模板一起留空」，那是階段 1 當下的狀態，後來補上了卻沒有改回來——以致於階段 10 查刻度提示時，一度以為是型錄沒資料。

**待核實的只剩沖煮手法的分段模板數值**：`duration` 最初是「舊定義（含注水）減 15 秒、悶蒸減 5 秒」估算出來的停水時間（`20260914110000_brew_method_hold_durations.sql`），之後依台灣主流教學交叉比對調整過一次（`20260917100000_brew_method_hold_durations_tw.sql`），**兩次都是依網路資料的估算，尚未經實機沖煮核實**。其餘 seed（處理法、品種、風味標籤、濾杯、手沖壺清單）依規格建立。

**階段 5 與階段 8 最容易出錯。** 前者是分段注水的資料模型，後者是手機上的表格。這兩個階段請多花時間，並主動說明你如何驗證。

**階段 10 之前不需要追求視覺完美。** 先讓功能跑通，視覺統一放最後處理。
